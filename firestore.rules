rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Colección ed_data: documentos maestros, alumnos, clases, etc.
    // Permite leer y escribir (la app no usa Firebase Auth).
    match /ed_data/{document} {
      allow read, write: if true;
    }
  }
}
